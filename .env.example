# Copy this file to .env (do NOT commit .env)
# Fill in your Edamam API credentials
REACT_APP_EDAMAM_ID=your_edamam_app_id
REACT_APP_EDAMAM_KEY=your_edamam_app_key

# Postgres (required for server-side saved recipes)
# Example: postgres://user:password@localhost:5432/recipes_db
DATABASE_URL=postgres://user:pass@localhost:5432/dbname

# JWT secret for signing tokens (set a strong secret in production)
JWT_SECRET=your_jwt_secret_here

# Example server-side env (if you add server-side features)
# SECRET_API_KEY=your_secret_here

# Optional: Social OAuth redirect/entry points for sign-in (set to your server endpoints or a test callback)
# Example local test: http://localhost:3000/oauth-sim.html?token=demo-google
REACT_APP_GOOGLE_AUTH_URL=
REACT_APP_GITHUB_AUTH_URL=
